### HU-013 — Tests de matriz sistemática de combinaciones (B12)

**Como** QA Lead responsable de la cobertura de pruebas
**Quiero** tests parametrizados que cubran el producto cartesiano estado × campo × iteración
**Para** garantizar que las CRs funcionan correctamente para todos los estados posibles y no solo los casos base verificados en B5

| # | Criterio de aceptación |
|---|---|
| AC-1 | **Given** que existen 7 estados de backlog y 5 campos base obligatorios, **When** se ejecutan los 35 tests de la submatriz 1 con `@pytest.mark.parametrize`, **Then** todos deben confirmar que cada campo base es obligatorio para cada estado de backlog **And** el output debe ser `35 passed` sin ningún skip |
| AC-2 | **Given** que existen 5 tipos de iteración backlog (`None`, `""`, `"Product Backlog"`, `"backlog"`, `"Mi Backlog Sprint"`), **When** se ejecutan las submatrices 6 y 7 (5 tests cada una), **Then** todos deben confirmar que CR-002 y CR-003 aplican para backlog por iteración **And** no solo para backlog por estado |
| AC-3 | **Given** que la suite completa tiene 210 tests (103 previos + 107 nuevos), **When** se ejecuta `PYTHONPATH=.:src poetry run pytest src/tests/unit_tests -q`, **Then** el resultado debe ser `210 passed` **And** el tiempo de ejecución debe ser inferior a 5 segundos |

---
